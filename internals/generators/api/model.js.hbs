const sequelize = require('../../../config/sequelize');
const Sequelize = require('sequelize');

const  {{  name }} = sequelize.define(' {{  name }}', {
  id: {
    type: Sequelize.INTEGER,
    allowNull: false,
    primaryKey: true,
    autoIncrement: true
  },
}, {
  tableName: '{{ properCase name }}s',
  timestamps: false,
});


module.exports =  {{  name }};

