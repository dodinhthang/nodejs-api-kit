// eslint-disable-next-line new-cap
const router = require('../../../node_modules/express').Router();
const {{ name }}Ctrl = require('./{{ name }}.controller');

router
    .route('/')
    .get({{ name }}Ctrl.list)
    .post({{ name }}Ctrl.create)

router
    .route('/:{{ name }}Id')
    .get({{ name }}Ctrl.get)
    .delete({{ name }}Ctrl.remove)
    .put({{ name }}Ctrl.update);
router.param('{{ name }}Id', {{ name }}Ctrl.load);


module.exports = router;
